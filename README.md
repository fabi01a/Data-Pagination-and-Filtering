This is a small project designed to practice data pagination, using event handlers, and accessing the DOM elements. 
The objective is to display at max, 9 student profiles per page with functioning pagination buttons at the bottom of
each page for navigation. The code is designed to work with a data set of any size, dynamically creating the 
appropriate number of pages based on the number of student objects provided.
